---

---

<section class="relative py-16 px-4 flex items-center justify-center bg-gradient-to-br from-blue-50 via-gray-100 to-gray-50 overflow-hidden">

  <!-- Fondo decorativo -->
  <div class="absolute inset-0 opacity-20 pointer-events-none">
    <img src="/low-poly-grid-haikei.svg" alt="Fondo decorativo" class="w-full h-full object-cover" />
  </div>


  <!-- Contenido -->
  <div  id="about" class="relative z-10 max-w-5xl w-full bg-white rounded-xl shadow-2xl flex flex-col md:flex-row items-center md:items-stretch overflow-hidden">
    <!-- Imagen -->
    <div class="flex-1 flex items-stretch">
      <img src="/" alt="Equipo" class="w-full h-full rounded-xl object-cover" />
    </div>
    <!-- Contenido del sobre nosotros -->
    <div class="flex-1 flex flex-col justify-center p-8 md:p-12">
      <h2 class="mb-4 text-4xl md:text-5xl font-bold text-blue-700 titulo-animado">¿Quiénes Somos?</h2>
      <p class="mb-2 text-xl text-gray-800 font-semibold">ENGLISH SCHOOL BY MARY POPPINS INTERNACIONAL CARE</p>
      <p class="text-lg leading-relaxed text-gray-700 font-[Roboto] parrafo-animado">Somos un instituto de idiomas con énfasis en la enseñanza mediante laboratorios reales de música, cine y cocina, destacándonos en prácticas de un 70% y solo un 30% teórico, ofreciendo de esta manera diversidad, pronto conocimiento y emociones.</p>
      <button class="px-8 py-3 mt-8 font-semibold text-white bg-blue-700 rounded-full shadow-lg hover:bg-blue-800 transition hover:scale-105">¿Quieres Saber Más?</button>
    </div>
  </div>
</section>

<script>
  import gsap from 'gsap'
  import { ScrollTrigger } from 'gsap/ScrollTrigger'
  gsap.registerPlugin(ScrollTrigger)
  //Animacion de scroll
  if (window.matchMedia("(min-width: 1280px)").matches) {
    


  gsap.from("#about",{
    scrollTrigger:{
      trigger:"#about",
      start:"top 65%",
      toggleActions: "play none none none"
    },
    
    y: 50,
  scale: 0.9,
  opacity: 0,
  duration: 1.2,
  ease: "power2.out"
    
  });

  //Animación optimizada: solo el título completo
  gsap.from(".titulo-animado", {
    scrollTrigger: {
      trigger: ".titulo-animado",
      start: "top 60%",
      toggleActions: "play none none none"
    },
    opacity: 0,
    y: 50,
    duration: 1.2,
    ease: "power2.out"
  });

  //Animacion de letras del parrafo
  gsap.from(".parrafo-animado", {
    opacity: 0,
    y: 50,
    stagger: 0.03,
    duration: 4,
    ease: "power3.out"
  });
}

</script>

